apiVersion: v1
kind: Service
metadata:
  name: bookinfo-db
  labels:
    app: bookinfo-db
spec:
  selector:
    app: bookinfo-db
  ports:
    - port: 5432
      targetPort: 5432
  type: NodePort
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bookinfo-db-pvc
  labels:
    app: bookinfo-db
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: bookinfo-db
  labels:
    app: bookinfo-db
spec:
  selector:
    matchLabels:
      app: bookinfo-db
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: bookinfo-db
    spec:
      containers:
      - image: tmaxcloudck/bookinfo-db:v0.0.1
        name: bookinfo-db
        imagePullPolicy: Always
        env:
        - name: POSTGRES_HOST_AUTH_METHOD
          value: "trust"
        volumeMounts:
        - name: bookinfo-db
          mountPath: /var/lib/postgresql/data
      volumes:
      - name: bookinfo-db
        persistentVolumeClaim:
          claimName: bookinfo-db-pvc
