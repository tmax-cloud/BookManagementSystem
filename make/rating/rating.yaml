kind: Service
apiVersion: v1
metadata:
  name: bookinfo-rating
  labels:
    name: bookinfo-rating
spec:
  selector:
    app: bookinfo-rating
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: bookinfo-rating
spec:
  selector:
    matchLabels:
      app: bookinfo-rating
  replicas: 1
  template:
    metadata:
      labels:
        app: bookinfo-rating
    spec:
      containers:
        - name: bookinfo-rating
          image: tmaxcloudck/bookinfo-rating
          imagePullPolicy: Always
          envFrom:
          - configMapRef:
              name: bookinfo-rating
          volumeMounts:
          - name: app-properties
            mountPath: /app/application.properties
            subPath: application.properties
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
      volumes:
      - name: app-properties
        configMap:
          name: bookinfo-rating-app-properties